== and .equals diff

== operator compares address/refreences
1. if the int a = 10; int b = 10; a==b it returns true because for primitives == compares value.
2. the same comparision obj a, b; a==b return false. Because it compares references
3. to check the value of the objects use a.equal(b) it returns true/false based on the content or value from object.